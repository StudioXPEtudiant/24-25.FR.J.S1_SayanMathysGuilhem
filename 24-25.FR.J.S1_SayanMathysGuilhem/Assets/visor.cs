using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class visor : MonoBehaviour
{
    //[SerializeField] private KeyCode visorKey = KeyCode.Mouse1;
    private GameObject move;

    private void Update()
    {
       // if (Input.GetAxis(visorKey))
        //{
            move.transform.position = new Vector3();
       // }
      //  else
       // {
           // move.transform.position = new Vector3(0.4788614, -0.199, 1.049255);
      //  }
    }
}
